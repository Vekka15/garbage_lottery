<% if current_user.nil? %>
  <%= link_to "Sign up", new_user_session_path%>
  <%= link_to "Sign in", new_user_registration_path %>
<%else %>
  <% @assignations.each do |a| %>
    <div>Day: <%= a.day.strftime("%A") %> </div>
    <div>Who: <%= a.user_mail %> </div>
  <%end%>
<%end%>
